import 'package:flutter/material.dart';
import 'package:simplex/common/all_common.dart';

Container notesView(BuildContext context) {
  return homeArea([
    homeHeader('To-Do', () {
      // TODO: Navigate to new to-do
    }),
  ]);
}
