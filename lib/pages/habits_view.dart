import 'package:flutter/material.dart';
import 'package:simplex/common/all_common.dart';

Container habitsView(BuildContext context) {
  return homeArea([
    homeHeader('Nuevos hábitos', () {
      // TODO: Navigate to new habit
    }),
  ]);
}
